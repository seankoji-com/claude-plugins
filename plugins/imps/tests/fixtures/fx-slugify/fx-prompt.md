Implement `slugify(text)` in `fx_slugify.py`:

- lowercase the text
- replace every run of non-alphanumeric characters with a single `-`
- strip leading and trailing `-`

The acceptance command is `python3 fx_slugify_test.py`; it must exit 0.
Do not edit `fx_slugify_test.py`.
