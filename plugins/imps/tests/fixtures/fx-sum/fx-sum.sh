#!/usr/bin/env bash
# Deliberately wrong on purpose: must total the integers on stdin, one per line,
# and print the sum. It currently prints 0 regardless of input.
#
# /imps opencode-harness fixture — see ../fx-add-two/fx_add_two.py for why the
# task is this small.
set -uo pipefail

total=0
echo "$total"
