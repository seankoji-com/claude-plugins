Fix `fx-sum.sh` so it reads integers from stdin, one per line, and prints their
total. Empty lines should be ignored.

The acceptance command is `bash fx-sum-test.sh`; it must exit 0.
Do not edit `fx-sum-test.sh`.
